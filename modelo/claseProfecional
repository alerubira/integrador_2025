class Profecional{
    constructor(idProfecional,idPersona,idProfecion,activoProfecional){
        this.idProfecional=idProfecional;
        this.idPersona=idPersona;
        this.idProfecion=idProfecion;
        this.activoProfecional=activoProfecional;
        }
        // Método para mostrar la información del usuario
    async alta(per) {
        await altaPersona(per);
        
    }
}